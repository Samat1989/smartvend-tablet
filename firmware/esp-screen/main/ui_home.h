// Экран привязанной машины: то, что видно, пока покупатель не подошёл.
//
// До появления нумпада (F5) он же служит признаком «прошивка живая и знает
// свой аппарат» — вместо отладочных полос этапа F2.
#pragma once

#include <stdbool.h>

void ui_home_show(void);

// Строка состояния внизу: сеть, каталог, ошибки. Зовётся из фоновых задач,
// блокировку LVGL берёт сама.
void ui_home_set_status(const char *text);
