// Мастер настройки на экране автомата: выбор сети, пароль, номер аппарата.
//
// Портала на телефоне у этой платы нет намеренно: экран и тач уже есть, и
// монтажнику не нужно искать SoftAP и держать в руках второй прибор.
#pragma once

#include <stdbool.h>

// Показывает мастер. Возвращается сразу — работа идёт в задаче LVGL.
void ui_setup_show(void);

// Ставит на текущий экран невидимую метку в углу: удержание 5 секунд
// открывает мастер заново. Так сервис попадает внутрь, а покупатель — нет.
void ui_setup_attach_service_corner(void);
