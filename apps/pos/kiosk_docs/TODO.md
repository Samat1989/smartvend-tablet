# 📋 TODO.md: Планшет-Киоск (Kotlin)

## Этап 1: Авторизация и Kiosk Mode 🔒
- [ ] **Экран авторизации**: UI для первичного ввода email и пароля агента маркета.
- [ ] **Локальное хранилище данных**: Интеграция `EncryptedSharedPreferences` для сохранения данных входа.
- [ ] **Фоновый логин**: Логика автоматического подключения к Supabase-аккаунту при запуске, используя сохраненные данные.
- [ ] **Режим Киоска (Kiosk Mode)**:
    - Вызов `startLockTask()`.
    - Скрытие Navigation Bar и Status Bar.
    - Перехват аппаратных кнопок ("Back", "Home").

## Этап 2: Интеграция с Базой (Supabase) 🗄
- [ ] **Слой данных (Models)**: Создание Data Classes: `Micromarket`, `InventoryItem`, `Sale`.
- [ ] **Выгрузка витрины**: Получение товаров (`inventory`) привязанных к текущему micromarket_id.
- [ ] **Realtime Синхронизация**: (Опционально) Слушать изменение цен через WebSockets от приложения владельца, чтобы обновлять их на экране онлайн.

## Этап 3: Покупка и Интеграция QR модуля 💳
- [x] **Модуль оплаты (QR)**: Использовать существующий код генерации/приема оплаты по QR.
- [ ] **Управление Корзиной**: Локальное состояние выбранных товаров (Cart State).
- [ ] **Таймаут очистки**: Если корзина собрана, но её не оплачивают X секунд — корзина сбрасывается.
- [ ] **Интеграция Суммы**: Передача итоговой суммы в уже реализованный QR-модуль.

## Этап 4: Фиксация продаж (Sales & Inventory) 📊
- [ ] **Запись транзакции**: При успешном API-callback от банка, отправляем `insert` в таблицу `sales` (Supabase).
- [ ] **Списание остатка**: Отправка `update` в `inventory`, уменьшая `stock` на количество купленного товара.
- [ ] **Оффлайн очередь**: Обработка ситуации "оплата прошла, а интернет для записи в Supabase отвалился". 

## Этап 5: Периферия (Дополнительно) 🔌
- [ ] **Удаленное открытие**: Чтение обновлений из таблицы `commands`. Если статус `pending` с типом `unlock_door` — отправляем Bluetooth/Serial сигнал на реле замка и меняем статус команды на `executed`.
